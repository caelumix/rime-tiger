# 变更日志

## 2026-09-10 - v1.0.0

首个公开版本

### 输入行为

- 对齐参考实现的候选、切分、选重、退格和提前上屏行为
- 增加 `/` 引导的分类符号候选，支持半角和全角斜杠
- 增加 `;` 引导的快捷符号输入，保留组合输入中的二选功能
- 支持补充语料和全码白名单明文编辑，重新部署后生效

### 性能

- 使用二进制紧凑词典，降低初始化时间和常驻内存
- 限制模型分页缓存，避免大页长期占用内存
- 复用增量解码格网，并只重建受退格和选重影响的末端
- 限制长句评分缓存，改善连续输入性能

### 可靠性

- 校验词典、模型和文本输入格式，并保留可定位的错误信息
- 模型缺失或加载失败时切换到确定性的无模型排序
- 输入替换失败时保留原输入，避免丢码
- 清理组件状态和模型生命周期，支持多会话独立运行
- 覆盖运行时与构建器的全部显式错误路径

### 构建与分发

- 源码保留明文码表和字频，不跟踪生成词典或模型二进制
- 打包前执行可移植验证，并在临时目录生成运行词典
- 构建和测试支持重复执行，并在失败时保留旧输出
