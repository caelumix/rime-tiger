# 变更日志

## 2026-09-12 - v1.1.0

跟进参考实现官方 2026-09-10 更新

### 输入行为

- 增加 `Tab`/`Shift+Tab` 高亮后锁定候选：继续输入字母固定该候选的文本与编码边界，
  开启提前上屏时提交已确认部分，退格到边界以内解锁
- 空码顶屏的唯一性与强置信判断计入合法的非首选重码单字，`hx` 后继续输入不再提前提交“心”
- 无候选时按空格只重置会话，不再改写组合输入
- 方案同步 `punctuator/digit_separators: ""`；数字与小数点仍由 Lua 处理

### 性能

- 孤立惩罚改为按路径增量累计并缓存在格网节点上，取代每次追加重算整句；
  切分串改为读取时惰性生成。两项合计使追加均值由 656 µs 降至 383 µs 级、
  退格均值由 185 µs 降至 47 µs 级（同语料 A/B，跨运行波动约 10%）；
  对参考实现的差距从追加 2.058 倍、退格 4.375 倍收窄到追加 1.15 倍、
  退格反快 1.17 倍
- 保留整串重算为 `model.reference_isolation_penalty`，新增 oracle 回归断言
  两条路径给出同一惩罚，漏传边字符或缓存失效会直接失败
- 峰值 RSS 30.2 MiB，比参考实现少用 14.4 MiB（32.3%）

### 未采纳的上游优化

- 一元组哈希表：本地 `score_cache` 已吸收重复查询，实测只多占约 0.75 MiB 堆
- 解码证据复用：在 48 到 128 码长输入的先无证据、后带证据解码循环上，
  与不启用时耗时差异在噪声以内，故撤除该缓存分支
- 剩余实现差异来自工程结构（模块拆分、显式策略参数、oracle 双实现）

### 测试与流程

- 解码对照语料增加 `otqm`、`otwqm` 和“题目”词组，并新增回退与锁定按键对照
- 解码回归覆盖 `has_complete_candidate` 的排除文本、组句资格和锁定参数、
  锁定解码不进入增量缓存，以及惰性切分串的字面量断言与锁定路径一致性
- 原生按键回归覆盖锁定、解锁、提前上屏锁定和空码顶点边界
- `tests/comparison.lua` 适配参考实现新增的 `decode` 位置参数
- 完整对照的解码部分按四个过滤/重码组合并行分片，合并计数与串行一致，
  完整对照耗时由十分钟级降至三分钟级
- 开发说明补充各项检查的耗时量级、依赖、`--quick` 使用时机与分片核对方法

## 2026-09-10 - v1.0.0

首个公开版本

### 输入行为

- 对齐参考实现的候选、切分、选重、退格和提前上屏行为
- 增加 `/` 引导的分类符号候选，支持半角和全角斜杠
- 增加 `;` 引导的快捷符号输入，保留组合输入中的二选功能
- 支持补充语料和全码白名单明文编辑，重新部署后生效

### 性能

- 使用二进制紧凑词典，降低初始化时间和常驻内存
- 限制模型分页缓存，避免大页长期占用内存
- 复用增量解码格网，并只重建受退格和选重影响的末端
- 限制长句评分缓存，改善连续输入性能

### 可靠性

- 校验词典、模型和文本输入格式，并保留可定位的错误信息
- 模型缺失或加载失败时切换到确定性的无模型排序
- 输入替换失败时保留原输入，避免丢码
- 清理组件状态和模型生命周期，支持多会话独立运行
- 覆盖运行时与构建器的全部显式错误路径

### 构建与分发

- 源码保留明文码表和字频，不跟踪生成词典或模型二进制
- 打包前执行可移植验证，并在临时目录生成运行词典
- 构建和测试支持重复执行，并在失败时保留旧输出
